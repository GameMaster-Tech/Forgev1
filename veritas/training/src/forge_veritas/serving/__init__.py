"""Serving — vLLM + Modal wrappers.

Modules ship in CP13 + CP14:
    vllm_app.py    — CP13, OpenAI-compatible local container (AWQ-int4)
    modal_app.py   — CP14, Modal asgi_app wrapping vLLM for production
"""
